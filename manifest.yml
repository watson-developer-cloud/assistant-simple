---
declared-services:
  my-conversation-service:
    label: conversation
    plan: Lite
applications:
- name: conversation-simple
  command: npm start
  path: .
  memory: 256M

  instances: 1
  services:
  - my-conversation-service
  env:
    NPM_CONFIG_PRODUCTION: false
