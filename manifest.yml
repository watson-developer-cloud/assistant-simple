---
declared-services:
  conversation-experimental:
    label: conversation
    plan: experimental
applications:
- services:
  - conversation-experimental
  command: npm start
  path: .
  instances: 1
  memory: 512MB